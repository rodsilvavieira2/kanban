// eslint-disable-next-line import/no-unresolved
import { defineConfig } from 'vite';

// https://vitejs.dev/config
export default defineConfig({
  // react plugin removed due to ESM requirement conflict with forge
});
